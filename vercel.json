{
  "cleanUrls": true,
  "buildCommand": "chmod +x ./node_modules/.bin/expo && npx expo export --platform web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
